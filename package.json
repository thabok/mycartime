{
  "name": "mycartime",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri desktop shell bundling the frontend and the Python backend sidecar",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
